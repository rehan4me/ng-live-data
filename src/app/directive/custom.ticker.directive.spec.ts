import { CustomTickerDirective } from './custom.ticker.directive';

describe('Custom.TickerDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomTickerDirective();
    expect(directive).toBeTruthy();
  });
});
